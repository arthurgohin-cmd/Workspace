DATABASE_URL="file:./dev.db"

# Générer avec : openssl rand -base64 32
SESSION_SECRET=""

# Compte administrateur unique, créé au premier `npm run db:seed`
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-me"

# Optionnel — active la traduction automatique FR->EN des fiches projets (https://www.deepl.com/pro-api).
# Sans clé, le texte anglais retombe sur le texte français saisi.
DEEPL_API_KEY=""
